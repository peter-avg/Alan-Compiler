chars () : int 

    (* To add1 should be added in the symbol table *)
    add1 (c : int) : int 
    
        a : int;
        {   
            a = c + 1;
            Print a;
            (* Something needs to be done with return, probably eval() *)
            return c;
    }

    c : int;
    a : int;
{
    a = 1;
    c = 1;
    (*  There needs to be Call(add1) -> eval() for it to run*)
    a = c + c;
    Print add1(a);
    return a;
}


